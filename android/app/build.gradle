apply plugin: "com.android.application"

android {
    compileSdkVersion 30

    defaultConfig {
        applicationId "com.easycodex.mobile"
        minSdkVersion 26
        targetSdkVersion 30
        versionCode 1
        versionName "0.1.0"
    }
}
